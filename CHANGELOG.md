# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，版本号形如 `MAJOR.MINOR.PATCH`。

## [未发布]

### 新增
- **中文水印 / 页码**（阶段 5 收尾）：`office_add_pdf_watermark` 与 `office_add_pdf_page_numbers`
  现在支持中文 —— 叠加式写入的绘制流里挂一份**只带用到的字形的字体子集**
  （Type0 + CIDFontType2 + FontFile2 + CIDToGIDMap + ToUnicode，与 `office_create_pdf` 复用同一套机制）。
  实测 3 页中文水印让 80 KB 的样本变 92 KB，**原字节作为前缀一个都没变**。
  新参数 `cjk_font_path` 可指定字体；不传时自动找本机字体。
- **`lib/pdf-font.js`**：把字体嵌入逻辑从 `pdf-writer.js` 抽出来，生成与叠加两条路共用
  （`buildPdfFont` 产出子集/CID 映射/宽度/ToUnicode，`buildType0FontObjects` 产出六个互相引用的 PDF 对象）。
- **中文 PDF：字体子集嵌入**（阶段 5 收尾）
  - `office_create_pdf` 现在支持中文等非 WinAnsi 字符：自动找本机字体（SimHei / 等线 / 仿宋 / 楷体 /
    微软雅黑 / 宋体 / Noto Sans SC，也可用 `cjk_font_path` 指定），**只保留用到的字形**后按
    Type0 + CIDFontType2 + FontFile2 + CIDToGIDMap + ToUnicode 嵌入。实测含 119 个汉字的 2 页 PDF 只有 28.7 KB。
  - 关键设计：**保留原始字形编号**（`loca` 长度不变、未用字形写零长度），因此不需要重排编号；
    复合字形递归闭包；maxp/hhea/hmtx 同步裁剪。
  - 独立验证：`npm run verify:pdf-cjk` 生成中文 PDF 后用 **Word 自带的 PDF 解析器**读回逐项比对，5/5 通过。
- **阶段 5 收尾：从零生成 PDF 与表格提取**
  - `office_create_pdf`：自写页面树 / 内容流 / 字体资源 / xref，同时写 `/ToUnicode`，因此生成的文本
    **可被提取**（自己与第三方解析器都能读回）；自动折行与分页、`\f` 强制分页、A3–Legal 与自定义尺寸、
    纵向/横向、元数据（非 ASCII 走 UTF-16BE+BOM）；写完立刻读回校验，页数或首行对不上就不留文件。
    **只支持 WinAnsi 字符集**（标准 14 字体，无需嵌入）；中文会被明确拒绝 —— 中文 PDF 的正解是先写 DOCX
    再 `office_convert_document` 转 PDF（宿主引擎带中文字体）。
  - `office_extract_pdf_tables`：按文本片段位置推断表格（行按 y 聚类、列按 x 聚类），不依赖框线；
    行容差按行距自动推导，用估算字宽区分「词间距」与「列间隔」，靠空格对齐的表格也能还原。
    实测在 LibreOffice 导出的真实 PDF 上还原正确，且不会把正文误判成表格。跨页合并、合并单元格、
    扫描件（需要 OCR）明确不做并写进返回值的 `not_done`。
- **阶段 6：本地 Office/WPS 联动**（默认关闭，需 `allowLocalAutomation: true`）
  - `office_detect_engines`：读注册表列出本机 Office/WPS 组件与版本；`probe_com=true` 时实测 COM 可用性。
  - `office_recalculate`：用真实引擎重算（Excel 公式缓存、Word 域）后另存新文件 —— 这是「公式不重算」的正解。
  - `office_rerender`：用真实引擎整份重写后另存新文件（修「Office 能打开但提示修复」的文件）。
  - 安全契约逐条落地：禁宏执行、禁外链/外模板更新、禁弹窗、独立进程、超时终止、只清理自己启动的进程、
    文件锁、JSONL 审计（不记绝对路径与文档内容）。详见 [docs/本地联动安全说明.md](docs/本地联动安全说明.md)。
- **`office_compare_docx`**：比较两份 Word 的段落级差异（只读，不写修订标记）——段落新增/删除/修改并区分
  「文字变了 / 样式变了 / 都变了」、表格个数与逐格文本变化、段落样式集合差异、元数据字段差异。
  两级对齐：一级最长公共子序列（中间插入不会把后面全报成修改），二级在「删+增」区块内按相似度
  （文字相似度 ×2 + 样式相同 ×1）做保序最大权匹配，因此能区分「这段改了文字」「这段只换样式」「这里真插了一段」。
  对齐超限时退化为按位置配对并明确告知；`max_items` 截断但 `total_*` 保真。

### 性能（阶段 7）
- **区域读取不再建整表 DOM**：`office_read_range` / `office_read_sheet` 改为直接扫工作表部件的原始字节，
  扫到区域最后一行之后立即停止。20 万单元格的表读 100 行：**764 ms → 118 ms**，且不再产生几百 MB 的 DOM。
- 共享字符串表与样式表**按需解析**（纯数字区域不再触碰 `sharedStrings.xml`）。
- 新增读取上限：单次区域 20 万格（`MAX_RANGE_CELLS`）、整表 50 万格（`MAX_SHEET_CELLS`），
  都是**在分配内存之前**拒绝并给出可操作建议。
- 新增写入上限：单表 30 万格（`MAX_EDITABLE_CELLS`，插件配置 `maxEditableCells` 可调）。
  写入必须把整表解析成 DOM，实测 50 万格会让宿主进程陷入 GC（>10 分钟不收敛），因此明确拒绝而不是硬写。

### 修复
- **`/W` 数组被当成 PDF 名称写出去，导致嵌入字体的字典非法**：自读完全正常，但 Word 的 PDF 解析器
  从这份 PDF 里读出 **0 个字**。原因是把宽度数组拼成了字符串，而序列化器把字符串当名称写
  （`/W /#5B1#201000…`）。改成真正的 PDF 数组（`[1 [1000] 2 [1000] …]`）后，Word 能完整读出中文。
  这条只有第三方解析器能发现 —— 记在任务清单的 bug 台账里。
- **`writeCells` 会让「行缺 `r` 属性」的工作表出现重复单元格**：同一个 `B2` 在文件里出现两次，
  并多出一个重复的 `<row r="2">`。现在按 ECMA-376 规则推算行号（上一行 + 1）并就地更新。
- **读取路径的 O(单元格数 × 字符串数) 退化**（本版本引入并当场修掉）：逐格重建共享字符串表，
  中型表读 100 行要 15 秒。改为「一趟扫描只解析一次」，并用零拷贝子视图把标签搜索限制在单元格区间内。

### 测试与文档
- 新增第 11 个套件 `npm run test:scan`（38 项）：与一份**独立的 DOM 参照实现**逐格等价比对；
  用「远处放一个非法引用」证明早停是真的（不靠计时）。
- 新增第 12 个套件 `npm run test:automation`（10 项）：用桩脚本验证本地联动的开关、超时、
  进程回收兜底、结果解析与审计（不启动 Office）；真机验证走 `npm run verify:automation`。
- 新增 `npm run fixture:xlsx-large`（直接拼 XML 造大样本，50 万格 1.7 s）与 `test/read-cells-check.mjs`（逐格读数对照）。
- 新增 `docs/OCR能力核查.md`：本机 OCR 能力的核查结论（内置 OCR 可用但插件够不着、无命令行工具、无 PDF 光栅化器），附命令与原始输出。
- 新增 `docs/本地联动安全说明.md`：本地自动化的逐条安全对照、审计字段、明确未做的边界。
- README 补「区域读取是真的按区域读」「文档比较」「本地 Office/WPS 联动」与诚实的边界。

## [0.1.0] — 2026-09-23

首个可用版本：**84 个工具**，覆盖 XLSX / DOCX / PPTX / PDF 的创建、读取、编辑、转换、校验与导出。
零运行时依赖（只用自己的 ZIP/XML/PDF 实现与 Node 内置模块）。

### XLSX
- 读写：工作表清单、稀疏单元格分页读取、A1 区域读取、批量写入（文本/数字/布尔/日期/公式）
- 结构：插入/删除行列、合并单元格、工作表增删改名、查找替换（默认跳过公式，支持 `dry_run`）
- 对象：Excel 表格（ListObject，含筛选与样式）、图表（柱状/条形/折线/饼图，含分类与数值缓存）
- 样式：字体/填充/数字格式/水平对齐（语义化参数，自动复用已有样式定义）
- 导出：xlsx 副本或 UTF-8 带 BOM 的 CSV（有损，明确警告）

### DOCX
- 读写：元数据、段落（样式/大纲级别）、表格、页眉页脚与页码域、批注、修订、全文
- 编辑：段落增删改、查找替换、段落样式、字符样式、分页符、书签
- 表格：建表（列宽与表头）、改写单元格、插行/删行（含保护性检查）、合并单元格、套用表格样式
- 图片：插入、缩放（`wp:extent` 与 `a:ext` 同步）、删除、**环绕方式**（行内 ↔ 四周型/上下型/浮于文字上方/衬于文字下方）
- 版式：纸张与方向、页边距、页码域（`PAGE` 字段）
- 导出：docx 原样复制（字节一致）或纯文本（UTF-8 带 BOM）

### PPTX
- 读取：结构/幻灯片/单页/版式与主题清单/全文
- 创建：**从零生成演示文稿**（自造主题、母版、版式，不依赖模板文件）
- 编辑：形状文本、增删排序、复制幻灯片、文本框、图片、表格、**原生图表**、切换版式、切换主题
- 导出：PPTX → PDF（先报告动画/切换/媒体/隐藏页的丢失项，再核对页数）

### PDF
- 读取：元数据、页数尺寸旋转、文本提取（ToUnicode，中文不乱码）、全文搜索、批注清单、表单字段、结构识别
- 页面：旋转、删除、重排（增量更新，原有字节不动）
- 叠加：水印与页码（ASCII）、**中文便签批注**（`/Subtype /Text`）
- 表单：填写文本/复选框/单选组/下拉（`/V`、`/AS`、`/NeedAppearances`）
- 对象图：重写（回收字节）、拆分、合并、图片提取
- 元数据：标题/作者/主题/关键词/生成器

### 统一能力
- 统一请求/响应信封：`request_id`、`document_id`、`output_file.sha256`、结构化错误码与解决建议
- 任务五件套：预览 → 执行 → 回滚 → 状态查询 → 取消（破坏性操作必须显式确认）
- 最小修改：未触碰的部件逐字节不变；增量更新不重写原文件
- 校验器如实区分「已检查」与「需渲染才能判定」，不假装通过
- 转 PDF：复用宿主内置 LibreOffice 引擎（可选依赖，解析不到时明确报 `OFFICE_NOT_INSTALLED`）

### 验证方式
- 10 个测试套件 / 633 项断言（含并发与文件锁）
- 真实软件交叉验证：Microsoft Excel / Word / PowerPoint 16、WPS 表格 / 文字 / 演示 12、
  Word 自带的 PDF 解析器（PDF Reflow）作为第三方裁判
- 发布前闸门：打包规范校验、敏感信息扫描、隐私扫描、打包产物装载自检、安装/升级/卸载演练

### 已知限制
见 [README「已知限制」](README.md#已知限制) 与 [任务清单「已知边界」](任务清单.md)。
