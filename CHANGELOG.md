# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，版本号形如 `MAJOR.MINOR.PATCH`。

## [0.1.0] — 2026-09-23

首个可用版本：**84 个工具**，覆盖 XLSX / DOCX / PPTX / PDF 的创建、读取、编辑、转换、校验与导出。
零运行时依赖（只用自己的 ZIP/XML/PDF 实现与 Node 内置模块）。

### XLSX
- 读写：工作表清单、稀疏单元格分页读取、A1 区域读取、批量写入（文本/数字/布尔/日期/公式）
- 结构：插入/删除行列、合并单元格、工作表增删改名、查找替换（默认跳过公式，支持 `dry_run`）
- 对象：Excel 表格（ListObject，含筛选与样式）、图表（柱状/条形/折线/饼图，含分类与数值缓存）
- 样式：字体/填充/数字格式/水平对齐（语义化参数，自动复用已有样式定义）
- 导出：xlsx 副本或 UTF-8 带 BOM 的 CSV（有损，明确警告）

### DOCX
- 读写：元数据、段落（样式/大纲级别）、表格、页眉页脚与页码域、批注、修订、全文
- 编辑：段落增删改、查找替换、段落样式、字符样式、分页符、书签
- 表格：建表（列宽与表头）、改写单元格、插行/删行（含保护性检查）、合并单元格、套用表格样式
- 图片：插入、缩放（`wp:extent` 与 `a:ext` 同步）、删除、**环绕方式**（行内 ↔ 四周型/上下型/浮于文字上方/衬于文字下方）
- 版式：纸张与方向、页边距、页码域（`PAGE` 字段）
- 导出：docx 原样复制（字节一致）或纯文本（UTF-8 带 BOM）

### PPTX
- 读取：结构/幻灯片/单页/版式与主题清单/全文
- 创建：**从零生成演示文稿**（自造主题、母版、版式，不依赖模板文件）
- 编辑：形状文本、增删排序、复制幻灯片、文本框、图片、表格、**原生图表**、切换版式、切换主题
- 导出：PPTX → PDF（先报告动画/切换/媒体/隐藏页的丢失项，再核对页数）

### PDF
- 读取：元数据、页数尺寸旋转、文本提取（ToUnicode，中文不乱码）、全文搜索、批注清单、表单字段、结构识别
- 页面：旋转、删除、重排（增量更新，原有字节不动）
- 叠加：水印与页码（ASCII）、**中文便签批注**（`/Subtype /Text`）
- 表单：填写文本/复选框/单选组/下拉（`/V`、`/AS`、`/NeedAppearances`）
- 对象图：重写（回收字节）、拆分、合并、图片提取
- 元数据：标题/作者/主题/关键词/生成器

### 统一能力
- 统一请求/响应信封：`request_id`、`document_id`、`output_file.sha256`、结构化错误码与解决建议
- 任务五件套：预览 → 执行 → 回滚 → 状态查询 → 取消（破坏性操作必须显式确认）
- 最小修改：未触碰的部件逐字节不变；增量更新不重写原文件
- 校验器如实区分「已检查」与「需渲染才能判定」，不假装通过
- 转 PDF：复用宿主内置 LibreOffice 引擎（可选依赖，解析不到时明确报 `OFFICE_NOT_INSTALLED`）

### 验证方式
- 10 个测试套件 / 633 项断言（含并发与文件锁）
- 真实软件交叉验证：Microsoft Excel / Word / PowerPoint 16、WPS 表格 / 文字 / 演示 12、
  Word 自带的 PDF 解析器（PDF Reflow）作为第三方裁判
- 发布前闸门：打包规范校验、敏感信息扫描、隐私扫描、打包产物装载自检、安装/升级/卸载演练

### 已知限制
见 [README「已知限制」](README.md#已知限制) 与 [任务清单「已知边界」](任务清单.md)。
