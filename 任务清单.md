# dsh-exp-office 任务清单

> 最后更新：2026-09-23 20:05（Asia/Shanghai）
> 当前阶段：**阶段 2（XLSX）已完成**、**阶段 3（DOCX）版式能力已补齐**、**阶段 4（PPTX）幻灯片操作已补齐**、阶段 5（PDF）进行中；阶段 0–4 全部完成并真实软件验证
> 总体状态：插件已装进 DSH `web` profile（**90 个工具**），启动路径有三道闸门；**E 块（大文件与流式）已完成**

## 〇、本轮进度（谷时 A / B / C / D 已完成，E 进行中）

| 能力 | 状态 | 证据 |
|---|---|---|
| D 块：隐私清洗 | ✅ 完成 | 13 个夹具的文档属性/批注/修订/人员部件里的真名 → 中性名；6 个测试脚本的本机路径 → `os.homedir()` 推导；文档里的用户名路径与 GitHub 身份 → 占位写法；新增**推前隐私扫描** `npm run verify:privacy`（本地模式表不入库 + 内置规则），复扫 0 命中 |
| D 块：发布包自检 | ✅ 完成 | `npm run verify:pack`：`npm pack` → 清单核对（必需文件齐全、没混入 test/与运行残留）→ 临时安装（零运行时依赖）→ **用安装后的代码跑真实 Cordis 装载：84 个工具** |
| D 块：并发与锁 | ✅ 完成 | 新增第 10 个套件 `npm run test:concurrency`（6 项）：并发写串行化且两边都落盘、持锁时报 `FILE_LOCKED` 且文件逐字节不变、残留锁超时回收、并发读不阻塞 |
| D 块：安装/升级/卸载演练 | ✅ 完成 | `npm run verify:lifecycle`：安装（84 工具）→ 升级到 0.1.1 → 卸载（无残留）；**新增源码指纹断言**证明演练不改动工作区源码 |
| D 块：发版文档 | ✅ 完成 | 新增 `CHANGELOG.md`（0.1.0 全量能力与已知限制）与 `docs/发布检查单.md`（6 步 + 本机踩坑表）；README 补「并发与文件锁」「安装/升级/卸载」两节 |
| **D 块：GitHub 发布** | ✅ **已发布** | 仓库 `wanghenry9/dsh-exp-office`（**public**）｜首次提交 `7aba6a9`（121 文件 / 30545 行）｜tag **`v0.1.0`**｜推前过三道闸门（隐私 0 命中、敏感 BLOCKER=0、打包规范 PASS）｜`git ls-remote` 复核远程 refs 与本地一致 |
| D 块：踩到并修掉的两个真问题 | ✅ | ① `file:<目录>` 依赖是**符号链接**，探针「改安装副本」实际改到了工作区源码（已还原并加指纹断言）；② 手工探针把假依赖写进了工作区 `package.json`（已清除，并加「零运行时依赖」断言） |
| PPTX `office_create_presentation` | ✅ 完成 | **从零生成**演示文稿（不依赖模板文件）：内容类型 / 包级关系 / presentation.xml / 母版（含必需 `p:clrMap`）/ 版式 / 主题（clrScheme+fontScheme+fmtScheme）/ 首页 / 文档属性全部程序化写出；主题名 `office-plugin`（自己的配色字体）；生成后自检不通过不留文件 |
| ├ 踩坑（真机才暴露） | ✅ | 主题里渐变停靠点的颜色**必须直接写在 `<a:gs>` 下**，包一层 `<a:solidFill>` 会让 PowerPoint **拒绝打开整个文件**。定位手法：把自家部件逐个换成 PowerPoint 自己写的同名部件，能打开的那次就说明被换掉的 XML 有问题（二分到 `fmtScheme` 一行） |
| ├ 真机验证 | ✅ | PowerPoint：`SLIDES 1`、`SLIDE_WIDTH_PT 960`、`DESIGN_NAME office-plugin`、`LAYOUT_1 标题幻灯片`；在其上继续增页/文本框/表格/图表后 `SLIDES 2` + `charttype=51`（值 3,1,2）无修复。WPS 演示：`SLIDES 2`、`TABLE_2_2 rows=2 cols=2`、`WPS_OFFICE_OK` |
| PDF `office_fill_pdf_form` | ✅ 完成 | AcroForm 四类字段（文本/复选框/单选组/下拉）：只改 `/V` 与 `/AS` + `/NeedAppearances`，**不生成外观流**；开状态名从 `/AP /N` 读出；**两遍式写入**（先全验再全写，失败不留半填状态）；中文值 UTF-16BE+BOM；只读字段需 `force`。新增 `read_pdf(detail="form")` 读回 |
| ├ 验证 | ✅ | 读回逐字段一致（含中文 `张伟`）、单选组只有 B 那个 widget 为开、`/I` 索引正确、增量更新不动原有字节、Word PDF 解析器正常（`REFLOW_PAGES 1 / WORDS 2`）；**填写后的视觉呈现无法验证**（无 Acrobat，已在 README 声明） |
| DOCX `office_export_docx` | ✅ 完成 | docx **原样复制（逐字节一致）** + 纯文本导出（UTF-8 带 BOM，记事本不乱码）；拒绝静默覆盖；非 docx 明确拒绝。Word 打开副本：段落 9 / 表格 1 / 页脚域类型 33 与源文件读数相同 |
| PPTX `office_set_theme` | ✅ 完成 | 换母版主题（配色/字体）。**踩坑**：直接指向别处在用的主题部件（备注母版的主题）会让 PowerPoint **拒绝打开整个文件**；改为**克隆新主题部件**后正常，来回切换不堆积部件 |
| ├ PowerPoint 真机验证 | ✅ | `DESIGN_NAME` 可读；把源主题的 accent1 改成红色后切换，`ACCENT1_RGB` 由默认值变为 **255**，客观证明换主题真的生效 |
| ├ 顺带修掉的真 bug | ✅ | ① `attr(doc.root, …)` 用在 `#document` 容器上 → 版式 `type` 与主题 `name` **恒为 null**；② 主题名里的**零宽字符**导致按名字匹配永远失败（现已在入口清理并保留 `raw_name`） |
| PPTX `office_duplicate_slide` | ✅ 完成 | 副本插到源页紧后面；复制页面关系部件但**丢掉备注页关系**（共用 `notesSlide` 会让 PowerPoint 报「需要修复」）；部件名/`sldId`/关系 id 全部取新 |
| PPTX `office_add_text_box` | ✅ 完成 | 写齐 `p:nvSpPr`/`p:spPr`/`p:txBody`；形状 id 取全页最大 +1；像素按 96 DPI 换算 EMU；`\n` 分段、字号（百分之一磅）、加粗、对齐 |
| ├ PowerPoint 真机验证 | ✅ | `SLIDES 5`、副本页 3 个形状、第 3 个形状文本 = 新增文本框内容；`HASNOTES_2=yes` / `HASNOTES_3=no`（源页备注保留、副本不引用） |
| └ WPS 演示交叉验证 | ✅ | 副本页第 3 个形状 `type=17`（文本框）、名称「文本框 4」、尺寸 `390x90pt`（= 520×120px） |
| DOCX `office_set_docx_page_layout` | ✅ 完成 | 纸张预设 A4/A3/A5/Letter/Legal + 自定义厘米；横向交换宽高并写 `w:orient="landscape"`，纵向不写 |
| DOCX `office_set_docx_margins` | ✅ 完成 | 已有 `pgMar` 逐项改写（只覆盖传入项）；新建时写齐七项；`pgMar` 按 CT_SectPr 顺序排在 `pgSz` 之后 |
| DOCX `office_insert_docx_page_number` | ✅ 完成 | `PAGE` 域（fldChar 三段式）写进页脚/页眉，可加前后缀与对齐；返回 `FIELD_NEEDS_UPDATE` 提示按 F9 刷新 |
| ├ Word 真机验证 | ✅ | `PAGE_WIDTH_PT 1190.55` / `PAGE_HEIGHT_PT 841.9`（42×29.7cm）、`ORIENTATION 1`、`MARGIN_TOP_PT 56.7`（2cm）、`MARGIN_LEFT_PT 70.8`（2.5cm）、`FOOTER 第 1 页`、`FOOTER_FIELDS 1`、`FOOTER_FIELD_TYPES 33`（`wdFieldPage`） |
| PDF `office_search_pdf` | ✅ 完成 | 页码 + 页内字符偏移 + 行号 + 上下文；按**字面量**匹配（正则元字符转义）；大小写不敏感走正则 `i` 而非 `toLowerCase()`（长度会变、偏移会错） |
| └ 独立交叉验证 | ✅ | Word 自带 PDF 解析器（PDF Reflow）数 `XLSX` → `MATCH_COUNT 2`，与插件 `total_matches 2`（第 2、3 页各一处）**逐项吻合** |
| **E 块：区域读取真早停** | ✅ 完成 | `office_read_range` 不再建整表 DOM：直接扫工作表部件的**原始字节**，扫到区域最后一行之后的第一个 `<row>` 就停；`office_read_sheet` 也走同一条扫描路径（只读操作完全不建 DOM） |
| ├ 早停的**功能证明**（不靠计时） | ✅ | 样本第 1000 行放一个非法引用 `r="???"`：读 `A1:B2` **不报错**（证明没走到第 1000 行）、整表读取**必报错**（证明前一用例不是空跑）、读 `A999:B1000` 报错（边界正确）。随机计时会在负载下骗人，这个不会 |
| ├ 与旧路径**逐格等价** | ✅ | 第 11 个套件 `npm run test:scan`（38 项）：测试里保留一份**独立的 DOM 参照实现**（改造前的算法），对 6 种单元格类型 / 缺 `r` 的 `<c>` / 缺 `r` 的 `<row>` / 自闭合行 / 共享公式 / CDATA / 前置空格 / 越界共享字符串下标 / 13 个区域，逐单元格 `deepEqual` |
| ├ 读取上限（分配内存前拒绝） | ✅ | `MAX_RANGE_CELLS=200000`：`A1:XFD1048576`（171 亿格）直接给 `MEMORY_LIMIT` + 「请缩小区域，或按行分段读取」；`MAX_SHEET_CELLS=500000`：整表读取**在扫描途中**报错并指向 `office_read_range`（不再先物化整表再拒绝） |
| ├ 写入上限（保护宿主进程） | ✅ | `MAX_EDITABLE_CELLS=300000`：写入必须把整表解析成 DOM（最小修改模式的前提），实测 5 万格 0.46 s/150 MB、10 万格 0.94 s/265 MB、20 万格 1.70 s/495 MB；50 万格实测会把 Node 拖进 GC 泥潭（>10 分钟未收敛），因此明确拒绝并给出出路，可用插件配置 `maxEditableCells` 调整 |
| ├ 大样本生成器 | ✅ | `npm run fixture:xlsx-large`（`--rows/--cols/--wide/--random/--out`）：**直接拼 XML**，50 万单元格 1.7 s / RSS 186 MB；旧的 `writeCells` 造 100 万格会 OOM（本机堆约 2 GB）——是造样本的方式不对，不是被测代码的问题 |
| └ 真机交叉验证 | ✅ | 真实 Excel 打开 50 万单元格样本：`USEDRANGE A1:J50000`、`SHEETS 大数据`、无修复提示；逐格对照 `A1/A2/B2/C2/J50000/C3` 与 `=C1*2` 公式，**6/6 与插件读数完全一致**（`test/read-cells-check.mjs` ↔ `office-open-check.ps1 -Cells`） |
| **F 块：`office_compare_docx`** | ✅ 完成 | 两份 Word 的**段落级差异**（只读、不写修订标记）：段落新增/删除/修改并区分「文字变了 / 样式变了 / 都变了」、表格个数与逐格变化、段落样式集合、元数据字段；`max_items` 截断但 `total_*` 保真；对齐超限时**退化为按位置配对并明确告知** |
| ├ 对齐质量（两级对齐） | ✅ | 一级 LCS（键=样式+文字）保证「中间插一段不会把后面全报成修改」；二级在「删+增」区块内按相似度（文字×2 + 样式×1）做保序最大权匹配，从而区分「这段改了文字」「这段只换样式」「这里真插了一段」——**不加第二级时会把新插入的段落说成旧段落被改** |
| ├ 真机交叉验证 | ✅ | Word 读 `report.docx` = 9 段、`report-edited.docx` = 10 段且第 2 段为「本段由插件插入」；插件比较结果 = 「新增 1 段『本段由插件插入』+ 1 处正文文字修改」，**与 Word 的说法逐项一致** |
| **F 块：OCR 能力核查** | ✅ 完成（结论写进 `docs/OCR能力核查.md`） | 本机**有** Windows 内置 OCR（`Windows.Media.Ocr`，唯一识别语言 `zh-Hans-CN`），**实测**读出了图片里的中英文；但命令行 OCR 工具（tesseract/ocrmypdf/ImageMagick/Poppler）与 Python OCR 包**全都没有**，插件运行时也够不着 WinRT；**且没有 PDF 光栅化器**（OCR 只吃位图）→ 插件不提供 OCR，结论 + 命令 + 原始输出都写进文档 |
| **G 块：阶段 6 本地 Office/WPS 联动**（你已授权） | ✅ 完成 | 三个工具：`office_detect_engines`（读注册表列引擎与版本，默认不启动程序）、`office_recalculate`（真实引擎重算后另存）、`office_rerender`（真实引擎整份重写后另存）。**默认关闭**，需 `allowLocalAutomation` |
| ├ 安全契约（规格 §十二.4 逐条落地） | ✅ | 禁宏（`AutomationSecurity=3`）、禁外链/外模板更新、禁弹窗（`DisplayAlerts=false` + `-NonInteractive`）、独立进程（结果走 JSON 文件，不把 COM 对象暴露给上层）、超时（默认 120 s → 杀进程树 → 状态文件兜底 cleanup）、**只清理自己启动的进程**（PID 差值，不碰用户开着的 Office/WPS）、文件锁、JSONL 审计（只记文件名/耗时/结果，不记绝对路径与文档内容） |
| ├ 真机实测 | ✅ | `npm run verify:automation`（7 项全过）：故意把 `=1+2` 的缓存写成 **999** → **Excel 重算后读到 3**、**WPS 重算后也读到 3**；Word / PowerPoint 重写后产物能被自身读取器解析且段落数/页数一致；**每次都断言源文件逐字节未变 + 无进程残留** |
| ├ 单测（不启动 Office） | ✅ | 第 12 个套件 `npm run test:automation`（10 项，桩脚本）：开关闸门、参数校验、结果解析、脚本报错、**没写结果文件 → 明确失败**、**超时 → 终止 + 补 cleanup**、审计不含绝对路径、临时目录清理 |
| └ 文档 | ✅ | `docs/本地联动安全说明.md`（逐条对照 + 审计字段 + 明确没做的：受限账户/沙箱、视觉回归）+ README 新增「本地 Office/WPS 联动」一节 |
| **H 块：阶段 5 收尾 · 从零生成 PDF** | ✅ 完成 | `office_create_pdf`：自写页面树 / 内容流 / 字体资源 / xref，**同时写 `/ToUnicode`**（因此生成的文本可被提取）；自动折行与分页、`\f` 强制分页、A3–Legal 与自定义尺寸、纵向/横向、元数据（非 ASCII 走 UTF-16BE+BOM）；写完立刻用自家读取器读回校验，页数或首行不符就**不留文件** |
| ├ 真机独立验证 | ✅ | Word 自带的 PDF 解析器读生成的样本：`REFLOW_PAGES 2`、`WORDS 63`、`MATCH_COUNT 1`（标记串 `PDF-FROM-SCRATCH-MARKER`）—— 第三方解析器能独立读出内容 |
| ├ 诚实边界 | ✅ | 标准 14 字体 ⇒ **只支持 WinAnsi**；中文被明确拒绝（列出具体字符）并指明正解：先写 DOCX 再 `office_convert_document` 转 PDF（宿主引擎带中文字体） |
| **I 块：阶段 5 收尾 · PDF 表格提取** | ✅ 完成 | `office_extract_pdf_tables` + `PdfDocument.textRuns()`：带坐标的文本片段（跟踪 `BT`/`Tm`/`Td`/`TD`/`T*`），再按 y 聚类成行、x 聚类成列还原表格 |
| ├ 三个真机才暴露的算法问题 | ✅ | ① 行容差写死 3 pt → 同一行里汉字与数字基线差 **4.9 pt** 就被拆成两行 ⇒ 改为**按行距自动推导**；② 只看「片段间距」会把正文（词间距 10.5 pt）误判成列 ⇒ 用**估算字宽**区分词间距与列间隔（阈值 1.2 em）；③ 靠空格对齐的表格整行只有一个片段 ⇒ 按两空格以上切虚拟列 |
| ├ 实测结果（真实 PDF） | ✅ | `sample.pdf` → `[["月份","金额"],["1月","12000"]]` ✅；`sample-multipage.pdf` → 只在第 3 页报出 3×3 表 ✅；无表格的 PDF（含本插件生成的）→ 0 张表，**不误判正文** ✅ |
| └ 独立交叉验证 | ✅ | Word 的 PDF 解析器在同一份 PDF 里同样读到单元格值 `12000`（`MATCH_COUNT 1`），证明提取内容不是我们编的 |
| DOCX `office_set_docx_image_wrap` | ✅ 完成 | 行内 `<wp:inline>` ↔ 浮动 `<wp:anchor>` 互转；写齐 anchor 必需属性、环绕元素插在 `wp:docPr` 前、位置元素插在 `wp:extent` 前；`tight`/`through` 明确拒绝 |
| └ Word 真机验证 | ✅ | `INLINESHAPES 0` → `FLOATSHAPES 1`、`SHAPE1_WRAP 0`（四周型）、`SHAPE1_POS 9,4.5`（EMU 精确）、`SHAPE1_SIZE 180x90`（尺寸不变） |
| PPTX `office_set_slide_layout` | ✅ 完成 | 换版式只改 `slideN.xml.rels` 里 `slideLayout` 关系的 Target（版式关联只由关系决定）；不挪动形状；新增 `read_pptx(detail="layouts")` 视图 |
| ├ PowerPoint 真机验证 | ✅ | `LAYOUT_1 空白 / LAYOUT_2 仅标题 / LAYOUT_3 空白 / LAYOUT_4 标题和文本`，且 `TITLE_1` 仍为「季度业务报告」（换版式不丢形状） |
| PPTX `office_export_presentation` | ✅ 完成 | PPTX → PDF（内置引擎）：导出前报告动画/切换/媒体/隐藏页丢失，导出后用自家 PDF 读取器核对页数；实测 4 页幻灯片（1 页隐藏）→ 3 页 PDF |
| └ 独立交叉验证 | ✅ | Word 自带 PDF 解析器读导出的 PDF：`REFLOW_PAGES 3`、`MATCH_COUNT 2`（`XLSX`），与插件一致 |
| 测试 | ✅ | 9 个套件 / **597 项断言**全绿（DOCX 133、PPTX 102、PDF 85、插件端到端 143） |
| DOCX 批注/修订只读视图 | ✅ 完成 | `office_read_docx` 新增 `detail="comments"` / `detail="revisions"`：批注含作者/日期/正文/锚点段落，修订逐条列出并区分「段落标记增删」与「删除文字」；线程化批注附加部件如实报告未解析 |
| ├ Word 真机交叉验证 | ✅ | `COMMENTS 1` / `COMMENT_1 author=审阅者 text=批注：这里的风险项需要法务复核。`、`REVISIONS 2`（`type=1` 插入文本一致、`type=2` 段落标记 Word 也读不出文字）、`BOOKMARK_1 name=RiskSection`——与插件读数逐项吻合 |
| DOCX `office_insert_bookmark` | ✅ 完成 | `bookmarkStart`/`bookmarkEnd` 成对插入，id 全文档唯一（重号会让目录与交叉引用失效）；名称按 Word 规则校验（含中文、不区分大小写地唯一） |
| └ Word 真机验证 | ✅ | `BOOKMARKS 2`、`BOOKMARK_1 name=title_mark text=书签验证文档`、`BOOKMARK_2 name=正文_1 text=本文件由 dsh-exp-office 生成。加粗片段` |
| PDF `office_add_pdf_annotation` | ✅ 完成 | 便签批注（`/Subtype /Text`），**内容支持中文**（UTF-16BE + BOM，由阅读器渲染，不需要嵌字体）；`/Annots` 是间接数组时解引用后追加（直接覆盖会丢原有批注）；写 `F=4` 打印标志；新增 `read_pdf(detail="annotations")` 读回视图 |
| ├ 验证 | ✅ | 读取器逐字段读回（含中文）+ 结构校验通过 + Word PDF 重解析正常（`REFLOW_PAGES 2 / WORDS 43`）+ 增量更新不动原有字节；**图标视觉未验证**（无 Acrobat） |
| PPTX `office_add_slide_chart` | ✅ 完成 | 原生图表（column/bar/line/pie）：图表部件 + 嵌入工作簿（用本插件自己的 XLSX 写入器生成）+ 图表关系 + 幻灯片关系 + `p:graphicFrame` + 内容类型六处接线；系列写 `c:strRef`/`c:numRef` + 缓存 |
| ├ PowerPoint 真机验证 | ✅ | `charttype=51/57/65/5`（柱/条/折线/饼）全部识别，系列数与数值（`120,150,180` 等）逐项吻合，标题存在，无修复提示 |
| ├ 踩坑（3 条，都是真机才暴露） | ✅ | `c:externalData` 必须排在 `c:spPr` 之后（否则 PowerPoint **拒绝打开**）；`graphicData uri` 写错会**静默丢图形**；系列只用 `strLit`/`numLit` 也会被拒绝打开 |
| 工具数 | ✅ | 80 个（`dsh.plugin.json` 与注册一致，`npm run verify:boot` 复验 0 漂移） |

**本轮抓到的新 bug（都是「看生成 XML」才发现的）**：给**没有 `w:pPr`** 的段落写页码时，
「先整段替换正文、再在正文起点插入 `<w:pPr>`」两个补丁的**起点相同**（零长插入不算重叠），
插入排在替换之后 → 正文被写两遍、`<w:pPr>` 落到 run 后面。
修法：一次补丁同时写 `pPr` 与正文；`pPr` 已存在时才分开改。已加回归用例。

**运维教训（已写进 README 与技术方案）**：本会话早期我在 **DSH 运行期间**反复在 profile 里跑 `pnpm install`，
它会按 `package.json` 收敛整棵 node_modules、并去替换被服务占用的原生包（报 `拒绝访问 os error 5`），
**牵连了 profile 里其它插件**（`dsh-context`、`dsh-ponytail-skills`、`dsh-whale-widget`、
`dsh-better-sidebar` 等目录被改写），随后整个插件树罢工。
现在同步安装副本一律用 `npm run sync:profile`（纯文件复制，只碰 `node_modules/dsh-exp-office/` 一个目录），
`pnpm install` 只在 DSH 停止时执行。详见 README「安装到 DSH profile」。

## 一、阶段进度（对照《开发要求》§十七）

| 阶段 | 目标 | 状态 | 证据 |
|---|---|---|---|
| 0 需求与技术验证 | 选型、可行性、样本库 | ✅ 完成 | [技术方案](docs/阶段0-技术方案.md)：自写 ZIP/XML 引擎、零运行时依赖 |
| 1 插件骨架 | 协议、工具契约、统一信封、事务 | ✅ 完成 | 90 个工具注册一致；真实 Cordis 装载测试；事务=锁→暂存→重开校验→原子替换 |
| 2 XLSX 核心能力 | 读写/样式/行列/工作表/表格对象/图表/导出/校验 | ✅ 完成 | 60 项适配器测试 + **38 项读取路径测试**（字节扫描与 DOM 参照逐格等价、早停证明、上限）；Excel 16 与 WPS 表格实测通过（ListObject 与图表对象都被识别） |
| 3 DOCX 核心能力 | 读写/段落/表格（含增删行·合并）/页眉页脚/图片/样式/页面设置/书签/批注修订读视图/**文档比较**/导出/校验 | ✅ 完成 | 143 项测试 + 23 项 Word 复杂样本保真；Word 实测（批注 1·修订 2·书签 1·目录 1·脚注 1 全保留）；比较结果与 Word 自己的段落读法一致 |
| 4 PPTX 核心能力 | 读写/**从零生成**/形状文本/增删排序/复制/文本框/图片/表格/原生图表/版式主题/导出 PDF/校验 | ✅ 完成 | 119 项测试；PowerPoint 实测（含图表类型/数值、版式名、主题配色）；WPS 演示交叉验证 |
| 5 PDF 核心能力 | 读取、文本提取、全文搜索、**表格提取**、**从零生成**、页面操作、叠加式编辑、便签批注、**AcroForm 表单填写**、重写/拆分/合并、图片提取 | ✅ 完成 | 112 项测试；真机独立验证：Word 的 PDF 解析器能读出从零生成的样本，并在同一份 PDF 里读到我们提取的表格单元格值 |
| 6 Office/WPS 联动 | 检测、重算、重渲染、弹窗处理、版本兼容矩阵 | ✅ 完成（本轮，已授权） | `office_detect_engines` / `office_recalculate` / `office_rerender`；真机：Excel 与 WPS 都把写错的公式缓存（999）重算成 3；默认关闭 + 逐条安全契约 + 审计；**受限账户/沙箱隔离与视觉回归未做**（见 docs/本地联动安全说明.md） |
| 7 性能、安全与发布 | 大文件、并发、兼容矩阵、发布包 | 🚧 大部分完成（已发布 v0.1.0） | 性能基准 ✅、**区域读取早停 ✅**、安全防护 ✅、安装/升级/卸载演练 ✅、发布包自检 ✅、隐私与敏感扫描 ✅；**100 MB 级流式（按行流式写/读）与多版本 Harness 兼容矩阵未做** |

## 二、2026-09-23 上午至今：XLSX 表格对象 + PDF 对象图重写（回收/拆分/合并）+ 图片提取

**XLSX 表格对象**：`office_create_table` 写 ListObject —— 表格部件、工作表 `tableParts`、工作表关系、
内容类型 Override 四处一次写齐；列名取表头，空/重复时把生成的列名写回表头单元格；同表内重叠拒绝。
Excel 16 与 WPS 表格 12 都认出表格（名字/区域/表头行/样式逐项一致）。
第一版把 `<tableParts>` 挂到文档节点，写出**两个根元素**的 XML：自家宽松解析器照样能读、测试全绿，
**Excel 直接判损坏** —— 修好后给 `validate_workbook` 补了「标签配平、单一根元素」检查与回归测试。

**PDF 四个工具**：`office_optimize_pdf`（重写为紧凑文件，可原地或另存）、`office_split_pdf`（按页拆出新文件）、
`office_merge_pdfs`（合并多份，跨文件搬运对象图）、`office_extract_pdf_images`（提取页面图片）。
实现要点：只写可达对象、重新编号、重建经典 xref 表；内容流保持原压缩字节；原地重写走事务，
另存/拆分/合并写完重开校验，不符就删产物报错；重写与合并共用「收集 + 重编号 + 写文件」两步。

图片提取单独一条线：JPEG/JP2 原样导出；Flate + 8 位分量还原预测器后包成 PNG（编码器在新的
`lib/image.js`）；索引色展开为 RGB、`/SMask` 合成透明通道；不支持的形态明确跳过并给原因。
独立验证用第三方解码器 sharp 打开导出的图并检查通道均值（样本纯色 (31,119,180)，实测一致）。

**这一轮踩到并修掉的三个坑**（都属于「结构合法、语义错了」）：

| # | 症状 | 根因 | 抓出方式 |
|---|---|---|---|
| 1 | 拆 1 页出来，字节里仍有 3 个页面对象（未选页内容没被清掉） | 书签 `/Dest`、命名目标、结构树 `/Pg` 直接指向被丢弃的页面，可达性遍历把它们又写了回来 | 加「`/Type /Page` 计数 == 页数」硬不变量 |
| 2 | 重写后目录与元数据静默丢失 | 新 trailer 写成 `/Root 1 R`，**漏了生成号**（规范要求 `num gen R`） | 元数据回读对比 |
| 3 | 某些文件重写后 `/Info` 丢失 | trailer 查找按「有没有 trailer 关键字」猜，没以 `startxref` 为准；且没处理**内联** `/Info` | 元数据回读对比 |
| 4 | 「明明有图，`structure().images` 却是 0」 | 流对象是 `{dict, stream}` 包装，结构统计直接读 `value.Subtype`，图片与对象流计数**恒为 0** | 造含图 PDF 样本后对账（为图片提取做准备时发现） |

顺带修了一个交互 bug：`rewrite()` 只读对象表，忽略「待写回」队列 → 「先改元数据再重写」会丢修改。

**验证方法论的重要纠正**（写进 README 了）：Word 报的 `PAGES` 是它**自己重排后的页数**，
不是 PDF 页数（实测 1 页排成 2 页、3 页排成 4 页）；页数一律以「插件读取 + xref 结构契约 +
`/Type /Page` 计数」为准。另外「搜不到被删页正文」不能当泄漏证据 —— 内容流是压缩的，正文不以明文出现。

## 三、本窗口（2026-09-23 早间）事故与修复

| 项 | 内容 |
|---|---|
| 现象 | `dsh web` 启动失败：`failed to apply loader entry dsh-exp-office ... TypeError: Invalid effect`，整个插件树加载失败 |
| 根因 | `apply` 末尾 `return { registered, manifest }` 返回普通对象；Cordis effect 契约只接受「清理函数 / null / undefined」 |
| 为何测试全绿 | 测试 `await apply(...)` 却从不检查返回值，**真实加载器路径从未被覆盖**（测试绕过真实契约） |
| 修复 | `apply` 不再 return（摘要改写 `ctx.logger.info`），源码与 profile 副本同步 |
| 补的闸门 | ① 返回值契约断言 ② **真实 Cordis 加载器装载测试** ③ **反例测试**（返回对象必须被拒） ④ `npm run verify:boot`：副本漂移 + 真实装载 + 工具数一致 |
| 副产物 | 明确「`dsh --profile web --dump-config` 不 apply 插件」，不能当启动检查；并记录 `dsh web` 运行时安全刷新副本的方法（别用 `pnpm install --force`，会撞上被占用的原生包） |

## 三之二、2026-09-23 晚间：我自己的操作事故（无损害，记账）

| 项 | 内容 |
|---|---|
| 现象 | 一条 `pwsh` 调用报 `Windows Job runner exited with exit code 4294967295`，命令没执行完 |
| 我做了什么 | 为清掉性能基准的残留 node 进程，执行了 `Get-Process node \| Where-Object { Id -ne 14484 -and Id -ne 14728 } \| Stop-Process -Force` —— **按名字粗暴过滤**，把该条命令自己的 job runner（也是 node）一起杀了 |
| 影响 | 只影响那条命令本身；DSH GUI（PID 14484，仍监听 3080）与 profile 子进程（14728）**未受影响**，会话继续正常 |
| 教训（已生效） | 不再按进程名批量杀进程：只用**明确 PID**，且先确认它不属于 DSH；清残留改用「等它自己结束 / 记下 PID」 |
| 顺带发现 | 性能基准一度「越跑越慢」其实是**两个 node 实例抢 CPU**（上一轮没真正退出）叠加代码退化，两件事混在一起；分开测才定位到真因 |

## 四、2026-09-22 下午完成项（上一窗口）

1. **PDF 页面级写操作**：`office_rotate_pdf_pages` / `office_delete_pdf_page` / `office_reorder_pdf_pages`
   —— 增量更新写回（原字节一个都不动，只在尾部追加新对象 + 新 xref + `/Prev` trailer）
2. **PDF 叠加式写入**：`office_add_pdf_watermark` / `office_add_pdf_page_numbers`
   —— 新建绘制流追加到 `/Contents`，base-14 Helvetica 不嵌字体，淡化用浅灰填充
3. **PDF 元数据写入**：`office_set_pdf_metadata`（`/Info`，非 ASCII 用 UTF-16BE+BOM）
4. **装进 DSH web profile**：`file:` 依赖 + `dsh.profile.bundles`，`dump-config` 可见
5. **WPS 演示交叉验证**：插件插入的图片与表格在 WPS 演示里逐项复核一致
6. 新增验证脚本 `test/pdf-open-check.ps1`（Word PDF 重排）

## 五、累计修掉的真 bug（都只有独立验证才暴露）

| # | 症状 | 根因 | 暴露方式 |
|---|---|---|---|
| 1 | PDF 写回后第三方阅读器读不出 | `startxref` 指向值自身而非 `xref` 关键字（差一个 `startxref\n`） | 结构契约测试（按 xref 链校验偏移）——自己的读取器是对象扫描，压根不看 xref |
| 2 | 删页「报告 2 页、读回 3 页」 | `pages()` 只按 `/Type /Page` 扫描，未把页面树 `/Kids` 当作唯一依据 | 删页后重新打开对比 |
| 3 | 加水印后原中文退化成控制字符 | 把 `/Font` 整个替换成只含 `Wm` 的字典，F1/F2… 全丢 | 读回提取文本 |
| 4 | 水印文字被 Word 静默丢弃 | `/Resources → 对象 → /Font → 对象 → /Wm → 对象` 多级间接引用 | 第三方阅读器（逐项隔离 4 个对照页定位） |
| 5 | 写页码后段落里正文出现两遍、`<w:pPr>` 落到 run 之后 | 「整段替换正文」与「在正文起点插入 pPr」两个补丁**起点相同**：零长插入不算区间重叠，插入排在替换之后 | 直接看生成的 `word/footer1.xml`（结构校验与自家读取器都判通过） |
| 6 | `office_duplicate_slide` 复制带备注的页会让 PowerPoint 报修复 | 副本共用源页的 `notesSlide` 部件 | PowerPoint 真机打开（`HASNOTES` 对比）+ 关系部件检查 |
| 7b | 复制含图表的幻灯片后 PowerPoint **拒绝打开**（不是提示修复） | 两页共用同一个 `chartN.xml` 图表部件；PowerPoint 自己复制时会新建一份 | PowerPoint 真机打开（自家校验通过、`structure().charts` 正常） |
| 8 | 版式 `type` 与主题 `name` 读出来**恒为 null** | `attr(doc.root, …)` 用在了 `#document` **容器**节点上；根元素是它的第一个 element 子节点 | 写主题功能时发现「三个版式都没有 type」，回头看 `layouts()` 同一个错误 |
| 9 | 换主题后 PowerPoint **拒绝打开整个文件** | 把幻灯片母版直接指向别处已在用的主题部件（本例是备注母版的主题），造成「双重引用 + 一个孤儿主题」 | 对照实验：手工换关系 → 打不开；只换主题内容 → 正常 |
| 10 | 按主题名匹配永远匹配不上，而错误信息看起来一模一样 | 简体中文 PowerPoint 在主题名后写了**两个零宽空格**（`U+200B`） | 打印码点才发现（`Office 主题` 长度是 11 而不是 9） |
| 11 | 从零生成的演示文稿被 PowerPoint **拒绝打开** | 主题 `fmtScheme` 里渐变停靠点写成了 `<a:gs><a:solidFill><a:schemeClr/></a:solidFill></a:gs>`；CT_GradientStop 要求颜色**直接**写在 `a:gs` 下 | 把自家部件逐个换成 PowerPoint 自己写的同名部件（整份主题 → clrScheme/fontScheme/fmtScheme 逐段），哪次能打开就锁定哪份 XML |
| 7 | 「浮动 → 行内」在插件层抛 `INTERNAL_ERROR: reading 'x'` | 适配器只在浮动分支返回 `offset_emu`，而工具 `summaryOf` 无条件读 `extra.offset_emu.x` | 插件级端到端用例（适配器测试不经过 `summaryOf`，全绿） |
| 12 | **自己引入又自己抓住的退化**：中型表读 100 行从 ~1 ms 变成 **15 秒** | 字节扫描里逐格调用 `#sharedStrings()`，而它每次调用都重建整张字符串表 → O(单元格数 × 字符串数)。改为「一趟扫描只解析一次」的上下文缓存 | 性能基准（小型表 100 行读数从 964 ms 掉到个位数毫秒才暴露）；**若只看功能测试会全绿通过** |
| 13 | 写「行缺 `r` 属性」的单元格 → 文件里同一个 `B2` 出现**两次**（旧值 + 新值），并多出一个重复的 `<row r="2">` | `writeCells` 建行索引时 `Number(attr(row,'r') ?? 0)` 直接跳过缺 `r` 的行，于是把这类行里的单元格当成「新行里的新格」。改为按 ECMA-376 规则推算行号（上一行 + 1），并让重复行号以第一个为准 | 新的读取路径测试（写 `A4` 后断言「`<row` 个数不变、`r="A4"` 只出现一次」）。**这是既有 bug，与新代码无关，靠新测试才浮出来** |
| 14 | 本地联动脚本在 Windows PowerShell 5.1 下**整个解析失败** | 脚本存成「无 BOM 的 UTF-8 + 中文注释/字符串」，PS 5.1 按 ANSI（本机 GBK）读，中文字节被拆成非法 token（`throw "链煡鍔ㄤ綔锛?Action"`） | 第一次直接跑脚本就报一串 `Unexpected token`。修法：**脚本正文只用 ASCII**，面向用户的中文全部由 Node 侧生成（与 `docs/OCR能力核查.md` 里记的是同一个坑） |
| 15 | 本地引擎报「找不到文件」，但文件就在那儿 | Office 自动化把**相对路径**解析成它自己的工作目录 | 真机跑 recalc 时报 `抱歉，无法找到 .dsh-release/recalc-in.xlsx`；修法：脚本里先 `Resolve-Path` 成绝对路径再交给引擎（输出路径先解析父目录再拼文件名） |
| 16 | 检测本机 Office/WPS 时「装了却报未安装」 | (a) 注册表 `LocalServer32` 的值带参数（`EXCEL.EXE /automation`）导致 `Test-Path` 失败；(b) WPS 把自动化服务器注册在 **WOW6432Node** 下，64 位视图里读不到 | 真机 detect 结果里 `installed=false` 但 `exe_path` 明显存在；修法：剥掉参数与引号、补查 `WOW6432Node`、再退回 Kingsoft 的 `InstallRoot` |
| 17 | 发布包自检「同一个脚本、同一个 tarball，`node test/pack-check.mjs` 通过，`npm run verify:pack` 报 `EALLOWSCRIPTS`」 | `npm run` 会把 `npm_config_*` 注入子进程，嵌套的 `npm install` 继承了外层配置（npm 11 在非交互下遇到带安装脚本的依赖会直接拒绝） | 两条命令对照跑：直接跑通过、经 npm 跑失败 → 修法：子进程环境里**剔除继承来的 `npm_config_*`**，并给安装加 `--ignore-scripts --omit=peer`（检查不该跑第三方脚本，也不该依赖网络装 peer） |

附带发现：多段 trailer 时读取取了**旧的**那段，导致「新建 `/Info`」回读看不见 —— 已改为取最新一段。

## 六、需要你决定的事

1. **重启 web profile**：安装副本已同步（0 漂移、90 个工具可装载），但当前 GUI 进程里仍是旧代码；
   重启后 90 个 `office_*` 工具才会出现（我没有重启你的服务）。
2. **本地联动的开关**：阶段 6 已按你的授权做完，但**默认关闭**。要用重算/重渲染，请在插件配置里加
   `allowLocalAutomation: true`（我也可以替你改 profile 配置，但那是你的运行环境，等你点头）。
3. **风险 #4**：行列移位是否要实现公式引用重写（当前只警告）。
4. **宏验证**：是否授权调整 Office「信任对 VBA 工程对象对象的访问」以做真实宏执行验证（当前只有合成样本，
   仅证明检测 + 字节保留；本地联动已按安全要求**禁宏**，不会执行宏）。
5. **`<c><v/></c>` 空值元素读出来是 `0` 而不是 `null`**：改造前就有的历史行为，我刻意保留了；
   要不要改成 `null`（语义变更）由你定。

## 七、环境与安全记录（对 `%USERPROFILE%\.dsh` 的全部改动）

- `profiles/web/package.json`：新增依赖 `"dsh-exp-office": "file:E:/deepseek harness/插件工作区/dsh-exp-office"` 与 `dsh.profile.bundles` 条目
- `profiles/web/node_modules/dsh-exp-office/`：pnpm 安装的插件副本（与工作区逐字节一致，`npm run verify:boot` 每次校验）
- 备份：`profiles/web/package.json.bak-dsh-exp-office-20260922-135229`、`pnpm-lock.yaml.bak-dsh-exp-office-20260922-135229`
- **未改动**：`profiles/web/cordis.patch.yml`（你自己的 patch 层）、`settings.yaml`、sessions、secrets、DSH 安装目录
- **披露**：pnpm 按 `package.json` 收敛时清理了 443 个遗留包（来源是两次被中断的插件安装，`.plugin-manager/logs/*/pnpm.log` 里 exit code 3221225786）；如需还原用上面的备份
- 每轮结束都确认：无残留 Office/LibreOffice 进程、无锁文件与 staging 目录、退出时释放文件句柄

## 八、已知边界（诚实清单）

**规格工具清单里本阶段明确没做的**（都写进 `lib/capabilities.js` 的 `not_implemented`，不静默缺失）：

| 规格项 | 为什么没做 |
|---|---|
| PDF `convert_pdf_to_docx` | 反向转换需要版面分析（段落/表格/图片重建），不是这一阶段能诚实交付的 |
| PDF OCR / 光栅化 | 需要 OCR 引擎与光栅化器，宿主内置引擎不提供 |
| XLSX 透视表修改 / 高级图表 | 透视表缓存结构复杂、高级图表（组合图/次坐标轴）超出当前图表构造器 |

**其他边界**：

- **斜向 45° 水印**的视觉效果未在第三方阅读器验证（Word 会丢弃旋转文字；本机无 headless PDF 渲染器）
- **PDF 元数据写入**只有结构校验 + 自身回读（Word 不映射 PDF 元数据，WPS PDF 的 COM 无法自动化驱动）
- **中文水印/叠加文字**需嵌入字体子集，未实现 → 明确拒绝而不是画乱码
- **页码/日期等域**不写死数字，需在 Word 里按 F9 刷新；WPS COM 的 `Fields.Count` 只统计正文域（页眉页脚里的域读不到，不代表没写进去）
- **图片环绕的 tight/through** 需要多边形包围盒，明确拒绝
- **重写后体积未必更小**：普通对象不再走对象流压缩，会返回 `NO_SIZE_GAIN` 提示
- **增量更新不回收字节**（要回收用 `office_optimize_pdf`）；被删页在增量路径下仍留在字节里
- **读取按区域、写入按整表**：`office_read_range` 已经是 O(区域)，但**写入**仍要把整张表解析成 DOM（最小修改模式的前提）→ 单表 >30 万格默认拒绝（`maxEditableCells` 可调）
- **首次读取某工作表要解压整个部件**：50 MB 级表部件约 0.5–1 s，之后同进程复用缓存；真正的「边解压边解析」流式还没做
- **`<c><v/></c>`（空值元素）读出来是 `0` 而不是 `null`**：`Number('')` 的历史行为，改造前后一致；改成 `null` 属语义变更，等你决定
- 100 MB 级文件仍是「能读不能改」（读按区域、写要整表）
- 宏只做检测与字节保留，**不执行**

## 九、规格工具名对照（《开发要求》§三 共 100 个名字 → 本插件 85 个工具）

规格里给了 100 个工具名，我们有 79 个工具：**一部分是同族合并**（一个工具用 `detail` 出多种视图），
**少部分是明确不做的**。逐族对照如下（✅=有对应实现；🔀=合并进某个工具的 detail/参数；❌=未实现并写明原因）。

| 族 | 对照 |
|---|---|
| 文件管理（8） | ✅ `list_files`→`office_list_files`、`get_file_info`、`delete_file`、`create_document`、`convert_document`；❌ `upload_file` / `download_file`（DSH 里文件已在本地工作区，没有上传下载这个概念）；✅ `compare_documents`→`office_compare_docx`（本轮补上） |
| Excel（20） | ✅ 19 个一一对应（`office_read_workbook`…`office_validate_workbook`）；🔀 `create_formula` 合进 `office_write_cells` 的 `formula` 参数（写公式要同时给缓存值，合成一个工具才不会出现「写了公式没缓存值」的中间态） |
| Word（35） | ✅ 23 个一一对应（本轮补上 `compare_docx`→`office_compare_docx`）；🔀 `read_docx_metadata`/`_paragraphs`/`_tables`/`_headers`/`_footers`/`_comments`/`_revisions` 七个读视图合进 `office_read_docx` 的 `detail`（读一次文件解析一次，分七个工具等于把同一个包解析七遍）；🔀 `convert_docx_to_pdf` 合进 `office_convert_document`；❌ `export_docx`（本插件一律**就地写回**并自动备份，另存副本用文件复制即可） |
| PowerPoint（16） | ✅ **16 个全部一一对应**（本轮补上 `create_presentation`、`set_theme`、`add_slide_chart`）；🔀 `read_slide` 合进 `office_read_pptx` 的 `detail="slide"` |
| PDF（16） | ✅ 12 个一一对应（本轮补上 `extract_pdf_tables`）（含 `search_pdf`、`add_annotation`、`fill_pdf_form`、`split_pdf`、`merge_pdf`、`extract_pdf_images`）；🔀 `read_pdf_metadata`/`extract_pdf_text` 合进 `office_read_pdf` 的 `detail`；🔀 `add_pdf_text` 由 `office_add_pdf_watermark` / `office_add_pdf_page_numbers` 覆盖（叠加式写入；中文受限于字体嵌入）；🔀 `export_pdf` 由 `office_split_pdf` / `office_optimize_pdf --output_path` 覆盖；❌ `extract_pdf_tables`（表格还原需要版面分析）、`convert_pdf_to_docx` |
| 任务控制（5） | ✅ 五个一一对应（`office_preview_operation` / `execute` / `rollback` / `get_task_status` / `cancel_task`） |

**覆盖率：100 个规格名里 97 个有实现**（其中 12 个是同族合并，85 个一一对应），3 个未实现（`upload_file`/`download_file` 属不适用）且都写进了
`lib/capabilities.js` 的 `not_implemented`（不静默缺失）。另外插件多做了规格里没点名但实际必要的能力：
`office_add_pdf_page_numbers`、`office_set_pdf_metadata`、`office_optimize_pdf`、`office_extract_pdf_images` 之外的
`office_merge_pdfs`/`office_split_pdf` 组合、`office_set_docx_image_wrap`、`office_duplicate_slide` 等。

## 十、尚未完成的任务（2026-09-23 19:32 盘点）

### 10.1 规格工具名里还没实现 / 不适用（3 / 100，其中 2 个属不适用）

| 规格名 | 现状 | 我的判断 |
|---|---|---|
| `upload_file` / `download_file` | 未实现 | **不适用**：DSH 里文件已在本地工作区，插件按路径直接读写，没有「上传/下载」这一步 |
| ~~`extract_pdf_tables`~~ | ✅ 已实现（本轮） | `office_extract_pdf_tables`：按位置推断（行按 y 聚类、列按 x 聚类），实测在 LibreOffice 导出的真实 PDF 上还原正确；跨页合并与合并单元格仍明确不做 |
| `convert_pdf_to_docx` | 未实现 | 反向转换需版面重建；规格 §二十三 明确列为**不建议第一版做** |

### 10.2 规格 §十七 阶段目标里的欠账

| 阶段 | 欠账 | 说明 |
|---|---|---|
| 5 PDF | **基础 PDF 生成**（从零造一份 PDF）、**OCR 能力验证**、**表单能力验证** | 目标里写的是「支持基础 PDF 生成」「完成 OCR 和表单能力验证」；我们做的是「Office→PDF 转换」与「PDF 读写/批注/搜索」，**从零生成 PDF 没做**，OCR 与表单只做了「识别并如实报告」，没做能力验证 |
| 6 Office/WPS 联动 | **全部**：本地 Office/WPS 检测、重新计算与重新渲染、软件未安装/进程异常/弹窗处理、版本兼容矩阵 | 需要你授权本机自动化（当前只用 COM 做**只读验证**：打开→读数→关闭，从不参与文档生成） |
| 7 性能·安全·发布 | **大文件流式**（100 MB 级）、**并发测试**、**Harness 版本兼容验证**（只验过 0.1.6-alpha.2）、**日志/监控/审计**、**升级与卸载流程**、**发布包**（npm pack / GitHub 发布） | 已完成：性能基准（3 项达标）、安全防护（路径穿越/炸弹/加密/宏）、安装流程、README 里的支持范围与已知限制 |

### 10.3 规格明确「不建议作为第一版强制目标」的（可以不做）

PDF 高保真原位文本编辑、PDF→Office 完全还原、PPTX 动画完整编辑、Excel 透视表完整修改、
DOCX 修订记录的完整创建/接受/拒绝、Office 宏的执行与修改、复杂 SmartArt 编辑、
音视频与 OLE 对象完整处理、字体/主题/高级排版的跨软件完全一致。

### 10.4 做不了或明确拒绝的边界（不是 bug）

中文水印/叠加文字（需嵌入字体子集）、图片环绕 tight/through（需多边形包围盒）、
加密 PDF 改写、宏执行、公式重算（只写缓存值）、增量更新不回收字节（改用 `office_optimize_pdf`）。

### 10.5 验证缺口（实现了，但本机无法验证）

斜向 45° 水印的视觉效果、PDF 元数据写入的第三方确认、便签批注图标的外观与位置、
WPS 侧页眉页脚里的域计数（WPS COM 只统计正文域）、100 MB 级真实文件的性能。

### 10.6 需要你决定或授权的事

1. **重启 web profile**：安装副本已同步（0 漂移、90 个工具可装载），但当前 GUI 进程里仍是旧代码；
   重启后 90 个 `office_*` 工具才会出现（我没有重启你的服务）。
2. 是否授权 **Office/WPS 自动化**（阶段 6）：允许插件驱动本机 Office 做重算/重渲染（会真的启动 Office 进程）。
3. 是否授权调整 Office「信任对 VBA 工程对象模型的访问」，以做**宏执行**验证（当前只证明检测 + 字节保留）。
4. 行列移位是否要实现**公式引用重写**（当前只警告 `FORMULA_NOT_REWRITTEN`）。
5. **谷时（今天 18:00 开工）做什么**：已按「低风险→高价值→阶段 7」排好六块（见 §十二）。
   **A/B/C 三块已完成**（`create_presentation` 一次通过、没触发回退规则）；接下来是 D（发布与并发）、
   E（大文件与流式）、F（OCR 能力验证 + compare_docx）。
6. **发布（已定：public 仓库 `dsh-exp-office`、MIT、用 package.json 的描述做定位）**：
   - 你要求「公开前清除敏感数据」→ 已做只读侦察并把清洗清单写进 **§12.2**（11 个夹具的文档属性、2 个 Word 样本的批注/修订/人员部件、文档里的真机读数与 GitHub 身份、6 个脚本里的本机路径）。
   - **PAT** 已在 `%USERPROFILE%\.dsh\secrets\github.env`（我只读掩码），全程未回显、未写入任何入库文件；remote URL 与 `.git/config` 里无令牌痕迹（已核查）。
   - **三道闸门已全部落地并跑通**：规范校验无 BLOCKER、敏感扫描 BLOCKER=0、本地隐私扫描 0 命中。
   - **需求原文 `dsh-exp-office开发要求.txt` 默认不发布**（已 gitignore，本地保留）——要公开请说一声。

## 十二、谷时时段工作计划（2026-09-23 18:00 开工）

> **开工时间：今天 18:00**（你指定）。下表按「18:00 + 八小时」铺到次日 02:00；
> 如果你的谷时结束得更早或更晚，**E/F 两块是弹性块**，我按剩余时间压缩或顺延，A–D 不动。
>
> 每块的「完成定义」与白天一致：**实现 + 测试 + 独立验证（真机或第三方解析器）+ 文档 + 同步安装副本**，
> 缺一项就不算完成，宁可停下来说清楚，也不留半成品。

| 时段 | 块 | 目标 | 验证方式 | 中止/回退规则 |
|---|---|---|---|---|
| 18:00–18:05 ✅ **已完成** | **A 收尾补齐**（低风险） | `office_export_docx`（另存 docx 副本，字节一致 + 拒绝静默覆盖）；`office_set_theme`（换母版主题，明确标注「只能证明打开不报修复」） | Word 打开副本；副本与原文件字节比对；PowerPoint 打开不报修复 | 无风险项，做不完顺延到下一块 |
| 18:05–18:10 ✅ **已完成** | **B PDF 表单**（中风险，规格欠账） | `office_fill_pdf_form`：文本/复选框/下拉三类字段（`/V`、`/AS`、`/I`）+ `/NeedAppearances true`；新增 `read_pdf(detail="form")` 读回 | 自建含 AcroForm 的样本 → 逐字段读回断言；Word PDF 重排仍能解析（文件没写坏） | 外观流生成**明确不做**（需渲染字体），文档里写清「外观由阅读器重绘」 |
| 18:10–18:17 ✅ **已完成**（比计划早，未触发回退规则） | **C 从零建演示文稿**（高风险高价值，规格欠账） | `office_create_presentation`：程序化生成 theme + slideMaster + slideLayout（标题/空白）+ 首页，写齐内容类型、两级 `_rels`、`presentation.xml` | PowerPoint 打开（页数/版式名/标题）；WPS 演示打开；再用 `add_slide`/`add_text_box`/`add_slide_chart` 继续编辑（证明不是死文件） | **21:15 仍被 PowerPoint 拒绝打开 → 撤掉工具注册**（代码保留但标 `not_implemented`），结论写进本清单，不留半成品 |
| 18:17–18:42 ✅ **已完成（已发布 v0.1.0）** | **D 阶段 7：发布与并发**（低风险；先做 §12.2 隐私清洗） | `npm pack` → 临时目录安装 tarball → **用打包产物跑全量测试**；`CHANGELOG.md` + 发布检查单；并发写同一文件必须 `LOCKED` 且不破坏原文件；临时 profile 里演练安装→升级→卸载 | 打包产物测试全绿；并发用例断言第二次调用 `LOCKED` + 源文件字节不变；卸载后目录无残留 | 发布走**三道闸门**（规范校验 + 敏感扫描 + 本地隐私模式扫描），**推 `commit`/`push` 各要你确认一次** |
| 18:43–19:24 ✅ **已完成**（提前于计划） | **E 大文件与流式**（中风险，弹性） | ✅ 交付：`read_range`/`read_sheet` 改走**原始字节扫描 + 真早停**；读取上限（区域 20 万 / 整表 50 万，分配内存前拒绝）；写入上限 30 万格（保护宿主进程）；大样本生成器；第 11 个套件 `test:scan` | ✅ 三条都不靠计时：① 第 1000 行放非法引用 → 读前 2 行不报错 / 整表必报错；② 与独立 DOM 参照实现逐格 `deepEqual`（38 项）；③ 真实 Excel 打开 50 万格样本（`USEDRANGE A1:J50000`）+ 6/6 逐格读数一致 | ✅ 未触发回退：字节级保真测试全绿（60 + 17 项），没有为了早停动写入引擎 |
| 19:24–19:33 ✅ **已完成** | **F 规格欠账验证**（弹性） | ✅ 交付：`office_compare_docx`（段落/表格/样式/元数据差异，两级对齐，只读）+ OCR 能力核查结论（`docs/OCR能力核查.md`） | ✅ compare：Word 读到的段数与第 2 段文本，与插件比较结果逐项一致；OCR：命令 + 原始输出为据（含「第一次中文乱码是我自己脚本编码问题」的排查记录） | ✅ 未顺延：两块都做完 |
| 19:33–19:48 ✅ **已完成** | **G 阶段 6：本地 Office/WPS 联动**（你已授权；先落安全契约再写功能） | ✅ 交付：`office_detect_engines`（默认只读注册表）/ `office_recalculate` / `office_rerender`；固定模板的 `office-automation.ps1`（纯 ASCII、参数化、禁宏禁外链禁弹窗、超时清理、只杀自己启动的 PID）；`lib/automation.js`（开关/超时/审计）；第 12 个套件 `test:automation` + 真机 `npm run verify:automation` | ✅ 真机：故意写错的公式缓存 **999 → Excel 重算成 3、WPS 也重算成 3**；Word/PowerPoint 重写后结构一致；**每次断言源文件逐字节未变 + 无进程残留**；审计不含绝对路径 | ✅ 保住了「安全默认」：不加配置就一律 `AUTOMATION_DISABLED`；受限账户/沙箱与视觉回归**如实列为未做**，不含糊过去 |
| 20:05 起 ✅ **已完成** | **H/I 阶段 5 收尾：从零生成 PDF + 表格提取** | ✅ `office_create_pdf`（自写页面/内容流/字体/xref + `/ToUnicode`，写完自读校验）+ `office_extract_pdf_tables`（按位置推断，含 `textRuns` 坐标提取） | ✅ 从零生成：Word 的 PDF 解析器独立读出内容（`REFLOW_PAGES 2`、`MATCH_COUNT 1`）；表格提取：真实 PDF 还原出 `月份/金额/1月/12000` 与 3×3 表，**无表格的 PDF 不误判**，单元格值经 Word 解析器交叉确认 | ✅ 中文 PDF 走「DOCX → convert」正解已写进 README，不硬撑未实现的字体嵌入（`pdf.create.cjk`） |
| 收尾（弹性） | **收尾** | 全量回归 + 真机矩阵（Excel/Word/PowerPoint/WPS/PDF）+ `npm run sync:profile` + `verify:boot` + 文档 + 面板 | 12 套件全绿、0 漂移、90 工具注册一致 | 收尾不做新功能 |

### 12.1 D 块（GitHub 发布）的前置条件与预检结果

**已经替你做完的预检**（用 `github-release` 技能的两个脚本，只读，不改项目）：

| 闸门 | 结果 |
|---|---|
| 打包规范校验 `github-repo-check.ps1` | **PASS-WITH-WARNINGS**（无 BLOCKER）：已补 `.gitignore`（日志规则）与 `.gitattributes`；**剩两项等你**：LICENSE 选哪个、git 提交身份（`git init` 后我会用你已有的全局身份 `<你的 GitHub 用户名> / <你的 GitHub 隐私邮箱>` 配好） |
| 敏感信息扫描 `github-secret-scan.ps1` | 首次 **BLOCKER=2**：`lib/pdf.js` 里 `const token = …` / `const genToken = …` 被判为「密码赋值」。核实是**误报**（PDF 记号解析的局部变量，右侧是 `buf.subarray(...)`，没有任何字面量）。处理方式是**把变量改名为 `word` / `genWord`**（而不是给扫描开例外），复扫 **BLOCKER=0 / WARN=0** |

**开工前你需要准备三样东西**（都在你这边做，我不需要你把令牌贴进对话）：

1. **PAT 放到凭据文件**：路径 `%USERPROFILE%\.dsh\secrets\github.env`
   （模板：`%USERPROFILE%\.dsh\skills\github-release\assets\github-env.template`）。
   权限需要 **Administration: Read and write**（建仓库）+ **Contents: Read and write**（推送）。
   我全程只读它的掩码输出，绝不回显明文，也不写进 remote URL / `.git/config` / 任何入库文件。
2. **告诉我**：仓库名（英文，如 `dsh-exp-office`）、可见性（默认 **private**）、LICENSE（MIT / Apache-2.0 / 私有不授权）、
   以及一句话定位（README 与仓库 About 用）。
3. **一次沙箱放开**：`git push` 需要 `danger-full-access`（沙箱禁止 git 给传输助手建管道，会报
   `cannot create standard input pipe for remote-https`），到时我会说明理由再申请，只放开推送那几条命令。

**发布流程的两道闸门 + 两次确认**（技能铁律）：`git add` 后跑 `-StagedOnly` 的规范校验与敏感扫描 →
把完整文件清单给你看 → 你确认后才 `git commit` → 再单独确认一次才 `git push` 与打 tag（首次发布打 `v0.1.0`）。

### 12.2 公开仓库的隐私清洗清单（你选的是 **public**，这条必须先做完）

已经用「只读侦察」把个人数据的落点全找出来了（侦察脚本放在 `.dsh-release/`，已 gitignore，发布后即删）：

| # | 落点 | 命中 | 处理方式 |
|---|---|---|---|
| 1 | **夹具的文档属性**：11 个 `.pptx` / `.xlsx` 的 `docProps/core.xml` | `dc:creator` 与 `cp:lastModifiedBy` 是**真名** | 一次性清洗脚本（`.dsh-release/scrub-fixture-authors.mjs`）把两者改写为 `dsh-exp-office`，改完重跑全量测试 |
| 2 | **`word-complex.docx` / `word-complex-edited.docx`** | 除 core.xml 外还有 `word/document.xml`（`w:ins`/`w:del` 的 `@author`）、`word/comments.xml`（`w:comment@author`）、`word/people.xml`（`w15:person`） | 作者名统一改成 `审阅者`；**批注/修订的条数、日期、文本一律不动**（保真测试断言的是数量与内容，不是作者名） |
| 3 | `README.md` 的真机读数引用 | 带真名（`author=…` 那几行） | 改成清洗后的名字，保持「与插件读数逐项吻合」的结论不变 |
| 4 | `任务清单.md` | 真名 + **GitHub 提交身份**（用户名与 `…@users.noreply.github.com` 邮箱） | 身份具体值**删掉**，只留「已在本地配置，不写入仓库」；真名同 #3 |
| 5 | 文档与脚本里的本机路径 | `C:\Users\<用户名>\.dsh\…`、`E:\…\dsh-exp-office` | 文档改成 `%USERPROFILE%\.dsh\…` 与 `<插件目录>`；**脚本**（`test/profile-boot-check.mjs`、`sync-to-profile.mjs`、`plugin.test.mjs` 等 6 个文件）改成用 `os.homedir()` 推导 —— 既脱敏又更可移植，改完必须全量测试 |
| 6 | `dsh-exp-office开发要求.txt`（你的需求原文，1671 行） | 无个人信息，但那是**你的文档** | **默认不发布**：加进 `.gitignore` 本地保留；README 说明「本仓库不含需求原文」。你要公开就说一声 |
| 7 | `.dsh-release/`（扫描报告与侦察脚本） | 运行残留 | 已 gitignore，发布后整个删掉 |

**新增一道长期可用的闸门**：`test/release-privacy-check.mjs` —— 扫描全部**已跟踪**文件，
比对一份**不入库**的本地模式表（`.privacy-patterns.local`，已 gitignore）。模式表本身绝不提交，
否则等于把要藏的东西写进仓库。它与技能的「规范校验 + 敏感扫描」一起构成推前三连检。

**清洗后的验收**（缺一条都不推）：
1. `node test/release-privacy-check.mjs` → 0 命中；
2. `github-secret-scan.ps1 -StagedOnly` → BLOCKER=0；
3. `github-repo-check.ps1 -StagedOnly` → 无 BLOCKER；
4. `npm test` 9 套件全绿（清洗夹具后必须复跑，尤其 word-fidelity）；
5. `npm run sync:profile && npm run verify:boot` → 0 漂移、80 工具。

**开工前的三条护栏**（与白天事故教训一致）：
1. 全程**不跑 `pnpm install`**（DSH 在跑时只 `npm run sync:profile`）；
2. 每个块结束前必须 `npm test` 全绿 + 工作区无临时文件；
3. 真机验证脚本用完即关，收工时确认无残留 Office 进程与锁文件。

## 十三、复现命令

```bash
npm test                 # 12 个套件 / 714 项断言
npm run test:scan        # 区域读取：字节扫描与 DOM 参照逐格等价 + 早停证明 + 上限
npm run test:perf:large  # 性能基准（含 50 万单元格样本：读 100 行 162 ms；写入被规模闸门挡住）
npm run fixture:xlsx-large -- --rows 50000 --cols 10   # 造大样本（直接拼 XML，1.7 s / 192 MB）
npm run verify:xlsx-large                              # 真实 Excel 打开大样本（USEDRANGE A1:J50000）
node test/read-cells-check.mjs test/fixtures/large-50000x10.xlsx 大数据 A1 A2 B2 C2 J50000 C3   # 逐格读数与 Excel 对照
npm run test:pdf         # PDF 适配器（页面操作 / 叠加写入 / 重写与拆分）
npm run verify:boot      # profile 启动自检：副本漂移 + 真实 Cordis 装载（应为 90 个工具）
npm run verify:pdf       # 用 Word 的 PDF 重排独立读回样本
npm run fixture:docx-layout && npm run verify:docx-layout   # A3 横向 + 页边距 + 页码域，Word 读回页面设置
npm run fixture:pptx-slide-ops && npm run verify:pptx-slide-ops   # 复制幻灯片 + 文本框，PowerPoint 读回
pwsh -File test/wps-office-check.ps1 -Path test/fixtures/deck-slide-ops.pptx -Kind presentation
pwsh -File test/pdf-open-check.ps1 -Path test/fixtures/sample-multipage.pdf -Match XLSX   # 独立数关键词，交叉验证全文搜索
npm run fixture:docx-wrap && npm run verify:docx-wrap   # 图片环绕：Word 读回形状数与环绕类型
npm run fixture:pptx-layout && npm run verify:pptx-layout   # 换版式：PowerPoint 读回每页版式名
npm run fixture:docx-bookmark && npm run verify:docx-bookmark   # 书签：Word 读回书签名与范围
npm run verify:privacy && npm run verify:pack && npm run verify:lifecycle   # 推前三连检（隐私 / 打包 / 生命周期）
npm run test:concurrency   # 并发与文件锁（串行化 / FILE_LOCKED / 残留锁回收）
npm run test:automation  # 本地联动的 Node 侧（桩脚本：开关/超时/进程回收/审计）
npm run verify:automation  # 真机：Excel 重算 999→3、WPS 重算、Word/PowerPoint 重写、源文件不变、无残留
npm run fixture:pdf-created && npm run verify:pdf-created   # 从零生成 PDF：Word 的 PDF 解析器独立读回标记串
npm run fixture:pdf-form && npm run verify:pdf-form   # 表单填写：四类字段 + Word 重解析
npm run fixture:pdf-annotation && npm run verify:pdf-annotation   # 中文便签批注 + Word 重解析
npm run fixture:pptx-charts && npm run verify:pptx-charts   # 原生图表：PowerPoint 读回类型/系列/数值
```
